/**
 * Placeholder design tokens.
 * Wireframe doc section 3 specifies starting grayscale/low-fidelity —
 * colour and branding are explicitly out of scope until layout and
 * interaction are validated.
 */
export const theme = {
  colors: {
    background: '#FFFFFF',
    surface: '#F2F2F2',
    text: '#111111',
    textMuted: '#666666',
    border: '#CCCCCC',
    error: '#B00020',
  },
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
} as const;
