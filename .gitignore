# dependencies
node_modules/

# React Native
.expo/
ios/build/
ios/Pods/
android/app/build/
android/build/
android/.gradle/
android/app/.cxx/

# Xcode
ios/.xcode.env.local
*.xcworkspace
!default.xcworkspace
xcuserdata/
*.xcuserstate

# Android
*.hprof
local.properties

# Metro
.metro-health-check*

# Env
.env
.env.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/

# OS
.DS_Store

# Editors
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Project docs
docs/

# Dictionary generation pipeline (WL-102) — script is committed and
# re-runnable (npm run dictionary:generate); its inputs/outputs are not:
# ESDB source+build (~150MB) and the generated bundle (~34MB, pre WL-105
# repacking) are both reproducible from the script, not hand-authored.
.cache/
src/assets/dictionary/dictionary.json
src/assets/dictionary/manifest.json